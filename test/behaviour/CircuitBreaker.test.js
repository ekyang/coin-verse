const { Contract, Protocol } = require('../protocol/Bancor')

const CircuitBreaker = artifacts.require('CircuitBreaker.sol')

contract('CircuitBreaker', async () => {
  // TODO Most of all, we should decide to implement this feature or not
})